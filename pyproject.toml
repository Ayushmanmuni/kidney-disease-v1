[project]
name = "ckd-predict"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.3",
    "bcrypt",
    "supabase",
    "flask-login",
]

[tool.vercel]
entrypoint = "api.index:app"
